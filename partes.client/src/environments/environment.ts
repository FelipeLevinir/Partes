// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
    production: false,
    apiSSOURL: 'https://svrappd.camara.cl/SSO/CamaraDeDiputadosSSOUserDBWebApi/',
    apiLibraryURL: 'https://svrappd.camara.cl/CamaraDeDiputadosWebApi/',
    
   
    apiURL: 'http://localhost:5049/',
   
    SistemaId: 2,
    siteKey: "6LfsLSEcAAAAAKhJKdg0aMOCVcROlbhrZQoS-z3P"   //RECAPTCHAv3 WEBSITE KEY

};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
